import { ConttopDirective } from './conttop.directive';

describe('ConttopDirective', () => {
  it('should create an instance', () => {
    const directive = new ConttopDirective();
    expect(directive).toBeTruthy();
  });
});
